#ifndef PUSH_SWAP_LIBFT_H
#define PUSH_SWAP_LIBFT_H

#include "stdlib.h"

int	ft_atoi(const char *nptr);
size_t	ft_strlen(const char *s);
int *chars_to_ints(char **argv, int len);
char	*ft_itoa(int n);
char	*ft_strdup(const char *s);

#endif //PUSH_SWAP_LIBFT_H
